# Setup Implementation Worktree

Use the `using-git-worktrees` skill to set up an isolated development environment with verified environment, committed state, and passing tests before ANY implementation begins.
