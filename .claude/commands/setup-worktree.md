# Setup Implementation Worktree

Use the `setting-up-implementation-worktree` skill to set up an isolated development environment (local worktree or online branch) with verified environment, committed state, and passing tests before ANY implementation begins.
